#!/usr/bin/env bash
#MISE description="Run PyKMIP + Synology DSM tests (non-FIPS only)"
#USAGE flag "-v --variant <variant>" env="VARIANT" help="FIPS variant" default="non-fips" {
#USAGE   choices "fips" "non-fips"
#USAGE }
#USAGE flag "-l --link <link>" env="LINK" help="Linkage type" default="static" {
#USAGE   choices "static" "dynamic"
#USAGE }
set -euo pipefail
source "${MISE_CONFIG_ROOT}/.mise/lib/common.sh"
source "${MISE_CONFIG_ROOT}/.mise/lib/kms_server.sh"
source "${MISE_CONFIG_ROOT}/.mise/lib/nix_helpers.sh"
kms_init_env "${usage_variant:-non-fips}" "${usage_link:-static}"
setup_test_logging

if [ "$VARIANT" != "non-fips" ]; then
  print_error "PyKMIP tests require non-fips variant (got: $VARIANT)"
fi

export WITH_PYTHON=1
ensure_nix_shell

print_header "Running PyKMIP + Synology DSM tests"

REPO_ROOT="$(get_repo_root)"
bash "${REPO_ROOT}/.mise/scripts/test/test_pykmip.sh" --variant "$VARIANT" --link "$LINK"

print_success "PyKMIP tests completed"
