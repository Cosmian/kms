#!/usr/bin/env bash
#MISE description="Run secret backend integration tests against a local Cosmian KMS"
#USAGE flag "-v --variant <variant>" env="VARIANT" help="FIPS variant" default="fips" {
#USAGE   choices "fips" "non-fips"
#USAGE }
#USAGE flag "-l --link <link>" env="LINK" help="Linkage type" default="static" {
#USAGE   choices "static" "dynamic"
#USAGE }
set -euo pipefail
source "${MISE_CONFIG_ROOT}/.mise/lib/common.sh"
kms_init_env "${usage_variant:-fips}" "${usage_link:-static}"
setup_test_logging

print_header "Running secret backend tests: Cosmian KMS (${VARIANT_NAME})"

bash "${MISE_CONFIG_ROOT}/.mise/scripts/test/test_secret_cosmian_kms.sh" \
  --variant "${usage_variant:-fips}" --link "${usage_link:-static}"

print_success "Secret Cosmian KMS tests completed"
