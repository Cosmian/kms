#!/usr/bin/env bash
#MISE description="Run CEF audit export test (live server + jc validation)"
#USAGE flag "-v --variant <variant>" env="VARIANT" help="FIPS variant" default="fips" {
#USAGE   choices "fips" "non-fips"
#USAGE }
#USAGE flag "-l --link <link>" env="LINK" help="Linkage type" default="static" {
#USAGE   choices "static" "dynamic"
#USAGE }
set -euo pipefail
source "${MISE_CONFIG_ROOT}/.mise/lib/common.sh"
source "${MISE_CONFIG_ROOT}/.mise/lib/nix_helpers.sh"
kms_init_env "${usage_variant:-fips}" "${usage_link:-static}"
setup_test_logging
ensure_nix_shell

print_header "Running CEF export test (${VARIANT_NAME})"

REPO_ROOT="$(get_repo_root)"

# Delegate to the CEF test script
bash "${REPO_ROOT}/.mise/scripts/test/test_cef.sh" --variant "$VARIANT" --link "$LINK"

print_success "CEF export test completed"
