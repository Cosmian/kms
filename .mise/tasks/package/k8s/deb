#!/usr/bin/env bash
#MISE description="Build Debian (.deb) package for the Cosmian KMS Kubernetes Plugin"
set -euo pipefail
REPO_ROOT="${MISE_CONFIG_ROOT}"
exec bash "${REPO_ROOT}/.mise/scripts/package/package_k8s_plugin.sh" --format deb
