// Notification integration tests.
//
// Unit tests for the notification persistence layer (create, read, mark-read) live in
// `crate/server_database`. End-to-end SMTP tests require a live SMTP relay (e.g. Mailpit)
// and are covered by the `test_data/configs/server/test/smtp_notifications.toml` fixture.
