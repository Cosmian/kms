# Cosmian KMS Client

HTTP client library for communicating with the Cosmian KMS server. Provides type-safe, async KMIP operations with support for multiple authentication mechanisms (API keys, OAuth 2.0, certificate-based).

Used by the CLI and any custom integrations. See `cosmian_kms_client_utils` for shared request/response utilities used by both this crate and the WASM client.
