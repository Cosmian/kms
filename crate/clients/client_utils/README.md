# Cosmian KMS Client Utilities

Shared utilities for KMIP request/response handling, data format conversion (PEM, DER, base64, JSON), and client configuration. Used by both the native `client` and WASM `wasm` crates.
