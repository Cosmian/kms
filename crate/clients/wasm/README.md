# Cosmian KMS Client WASM

WebAssembly bindings for the KMS client. Enables browser-based KMS operations via `wasm-bindgen` and `js-sys`. Exposes async Promise-based API for key management, encryption/decryption, and signing.

Used by `ui/` for the web interface. Build managed from `ui/README.md`.
