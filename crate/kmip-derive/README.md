# KMIP Derive Macros

Procedural macros for automatic TTLV serialization/deserialization of KMIP data structures. Generates `Serialize` and `Deserialize` implementations following KMIP binary encoding specs.

Used by `cosmian_kmip` to reduce boilerplate in type definitions.
