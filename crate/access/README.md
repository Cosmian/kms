# Cosmian KMS Access

Access control types and authorization logic. Defines permission models (KMIP operations) and access control lists (ACLs) for controlling who can perform what operations on which objects.

Used internally by the server to enforce authorization. See `cosmian_kms_server` for integration points.
