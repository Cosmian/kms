---
name: Build all

on:
  workflow_call:
    inputs:
      toolchain:
        required: true
        type: string
  workflow_dispatch:

jobs:
  test-nix:
    name: Test on ${{ matrix.type }} - ${{ matrix.features }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        type:
          - sqlite
          - mysql
          - percona
          - mariadb
          - psql
          - otel
          - google-cse
          - redis
          - pykmip
          - wasm
          - xks
          - azure-ekm
          - ui
          - gcp-cmek
          - openssh
          - luks
          - veracrypt
          - jose
          - iris
          - secret_vault
          - secret_aws
          - secret_azure
          - secret_cosmian_kms
          - spire
          - kmip-go
        features: [fips, non-fips]
        exclude:
          # redis is exclusively for non-fips
          - type: redis
            features: fips
          # pykmip is exclusively for non-fips since P12 is used for TLS KMS server
          - type: pykmip
            features: fips
          # aws_xks relies on curl SigV4 + non-FIPS build flags
          - type: xks
            features: fips
          # azure_ekm is only run with non-fips
          - type: azure-ekm
            features: fips
          # openssh PKCS#11 tests force non-fips (EdDSA / full algorithm set)
          - type: openssh
            features: fips
          # luks disk-encryption PKCS#11 tests force non-fips
          - type: luks
            features: fips
          # veracrypt disk-encryption PKCS#11 tests force non-fips
          - type: veracrypt
            features: fips
          # jose (REST crypto + jwcrypto interop) runs only with non-fips
          - type: jose
            features: fips
          # iris requires non-fips (PKCS#12 + full TLS stack)
          - type: iris
            features: fips
          # secret_cosmian_kms runs against a local KMS server — works with both fips and non-fips
          # secret_vault, secret_aws, secret_azure require external services — run non-fips only
          - type: secret_vault
            features: fips
          - type: secret_aws
            features: fips
          - type: secret_azure
            features: fips
          # spire relies on the Vault API which is non-fips only
          - type: spire
            features: fips
        include:
          # Docker services required per test type (types without an entry need no containers)
          - type: psql
            docker: postgres
          - type: mysql
            docker: mysql
          - type: percona
            docker: percona
          - type: mariadb
            docker: mariadb
          - type: redis
            docker: redis
          - type: otel
            docker: otel-collector jaeger

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - uses: ./.github/actions/cleanup-runner

      - uses: ./.github/actions/setup-nix

      - uses: ./.github/actions/install-mise

      - name: Run docker containers
        if: matrix.docker
        run: |
          docker compose up -d ${{ matrix.docker }}

      - name: Test
        env:
          POSTGRES_USER: kms
          PGUSER: kms
          POSTGRES_PASSWORD: kms
          POSTGRES_DB: kms
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          KMS_POSTGRES_URL: postgresql://kms:kms@127.0.0.1:5432/kms

          MYSQL_DATABASE: kms
          MYSQL_ROOT_PASSWORD: kms
          MYSQL_HOST: localhost
          MYSQL_PORT: 3306
          KMS_MYSQL_URL: mysql://root:kms@localhost:3306/kms

          PERCONA_HOST: localhost
          PERCONA_PORT: 3307
          KMS_PERCONA_URL: mysql://root:kms@localhost:3307/kms

          MARIADB_HOST: localhost
          MARIADB_PORT: 3308
          KMS_MARIADB_URL: mysql://root:kms@localhost:3308/kms

          KMS_SQLITE_PATH: data/shared

          REDIS_HOST: localhost
          REDIS_PORT: 6379
          KMS_REDIS_URL: redis://localhost:6379

          # Google variables
          TEST_GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.TEST_GOOGLE_OAUTH_CLIENT_ID }}
          TEST_GOOGLE_OAUTH_CLIENT_SECRET: ${{ secrets.TEST_GOOGLE_OAUTH_CLIENT_SECRET }}
          TEST_GOOGLE_OAUTH_REFRESH_TOKEN: ${{ secrets.TEST_GOOGLE_OAUTH_REFRESH_TOKEN }}
          GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY }}

          # EDB Postgres Advanced Server TDE variables.
          # When EDB_SUBSCRIPTION_TOKEN is set the test suite starts the real
          # EDB Postgres 17 container (edb-tde service in docker-compose.yml)
          # and runs a live end-to-end TDE scenario.  Without it the Docker
          # test is skipped and only the PyKMIP/Thales KMIP simulations run.
          # Register for a free token: https://www.enterprisedb.com/accounts/register
          EDB_SUBSCRIPTION_TOKEN: ${{ secrets.EDB_SUBSCRIPTION_TOKEN }}

          # InterSystems IRIS mTLS integration test
          IRIS_LICENSE_KEY: ${{ secrets.IRIS_LICENSE_KEY }}

          # AWS secret backend variables (secret_aws test type)
          AWS_ACCESS_KEY_ID: ${{ secrets.KMS_CI_AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.KMS_CI_AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.KMS_CI_AWS_REGION }}

          # Azure Key Vault secret backend variables (secret_azure test type)
          AZURE_TENANT_ID: ${{ secrets.KMS_CI_AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.KMS_CI_AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.KMS_CI_AZURE_CLIENT_SECRET }}
          AZURE_KV_NAME: ${{ secrets.KMS_CI_AZURE_KV_NAME }}

          # Provide an authenticated token so mise can install tools from
          # GitHub releases without hitting the unauthenticated rate limit.
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -ex
          mise run test:${{ matrix.type }} --variant ${{ matrix.features }}

  hsm:
    name: HSM ${{ matrix.hsm-type }} - ${{ matrix.features }}
    runs-on: ubuntu-latest
    # proteccio and crypt2pay hardware do not support concurrent connections from multiple CI runs;
    # give them fixed concurrency groups so only one job runs at a time across all PRs.
    # utimaco and softhsm2 use a per-run group so they are never blocked by other PRs.
    concurrency:
      group: ${{ (matrix.hsm-type == 'proteccio' && 'hsm-proteccio') || (matrix.hsm-type == 'crypt2pay' && 'hsm-crypt2pay') || format('hsm-{0}-{1}', matrix.hsm-type,
        github.run_id) }}
      queue: max
      cancel-in-progress: false
    strategy:
      fail-fast: false
      matrix:
        hsm-type:
          - utimaco
          # - proteccio # on July 2026, Proteccio test-HSM is unavailable
          - softhsm2
          - crypt2pay
        features: [fips, non-fips]
        exclude:
          # parallel connections on proteccio is not supported
          - hsm-type: proteccio
            features: fips
          # Not required - testing non-fips is sufficient
          - hsm-type: crypt2pay
            features: fips

    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - uses: ./.github/actions/cleanup-runner

      - uses: ./.github/actions/setup-nix

      - uses: ./.github/actions/install-mise

      - name: Test
        env:
          # HSM
          PROTECCIO_IP: ${{ secrets.PROTECCIO_IP }}
          PROTECCIO_PASSWORD: ${{ secrets.PROTECCIO_PASSWORD }}
          PROTECCIO_SLOT: ${{ secrets.PROTECCIO_SLOT }}
          CRYPT2PAY_PASSWORD: ${{ secrets.CRYPT2PAY_PASSWORD }}
          # Google variables
          TEST_GOOGLE_OAUTH_CLIENT_ID: ${{ secrets.TEST_GOOGLE_OAUTH_CLIENT_ID }}
          TEST_GOOGLE_OAUTH_CLIENT_SECRET: ${{ secrets.TEST_GOOGLE_OAUTH_CLIENT_SECRET }}
          TEST_GOOGLE_OAUTH_REFRESH_TOKEN: ${{ secrets.TEST_GOOGLE_OAUTH_REFRESH_TOKEN }}
          GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY }}
          # OVPN for Crypt2Pay HSM tests
          OVPN_CONF: ${{ secrets.OVPN_CONF }}
        run: |
          mise run test:hsm-${{ matrix.hsm-type }} --variant ${{ matrix.features }}

  helm:
    name: Helm chart E2E (minikube)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          submodules: recursive

      - uses: ./.github/actions/install-mise

      - name: Install Helm
        uses: azure/setup-helm@v5

      - name: Start Minikube
        uses: medyagh/setup-minikube@v0.0.21
        with:
          driver: docker
          kubernetes-version: v1.30.0
          cpus: 2
          memory: 4096m

      - name: Test (lint + template + E2E deployment)
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          mise run test:helm --e2e

  windows-2022:
    # Windows is slow: only run on PRs (catches regressions before merge) and tags (release gate).
    if: github.event_name == 'pull_request' || startsWith(github.ref, 'refs/tags/')
    uses: ./.github/workflows/test_windows.yml
    with:
      toolchain: ${{ inputs.toolchain }}
      archive-name: windows

  # cargo publish is placed in test_all.yml to ensure the publication of crates is possible (no git deps, etc.). The publication will be done on tags only.
  cargo-publish:
    needs:
      - test-nix
      - hsm
      - helm
    uses: ./.github/workflows/cargo-publish.yml
    with:
      toolchain: 1.97.0
    secrets:
      token: ${{ secrets.CRATES_IO }}

  cleanup:
    needs:
      - cargo-publish
    if: always()
    uses: Cosmian/reusable_workflows/.github/workflows/cleanup_cache.yml@develop
    secrets: inherit
