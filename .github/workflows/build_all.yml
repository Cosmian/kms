---
name: Build all

on:
  workflow_call:
    inputs:
      toolchain:
        required: true
        type: string
      debug_or_release:
        required: true
        type: string
  workflow_dispatch:


jobs:
  build-and-test:
    uses: Cosmian/reusable_workflows/.github/workflows/build_all.yml@add-workflows
    secrets: inherit
