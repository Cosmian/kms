---
name: Packaging

on:
  push:
    tags:
      - '**'
  pull_request:
  workflow_call:
jobs:
  # Run packaging for PRs
  packaging:
    uses: ./.github/workflows/packaging.yml
    secrets: inherit
    with:
      toolchain: 1.90.0
