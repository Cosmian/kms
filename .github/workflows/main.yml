---
name: CI in debug

on:
  push:
    branches:
      - '**'
    tags:
      - '!**'
  workflow_dispatch:

jobs:
  main:
    secrets: inherit
    uses: ./.github/workflows/main_base.yml
    with:
      toolchain: 1.90.0
      debug_or_release: debug
      platforms: linux/amd64
