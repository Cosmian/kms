---
default: true

# default indent = 4 spaces
MD007:
  indent: 4
  ul_indent: 4

MD013:
  line_length: 240

MD024:
  # Allow heading duplication if under different parent headings
  siblings_only: true

MD025:
  # ADR files use frontmatter title: + an H1 body heading — do not double-count
  front_matter_title: ''

MD001: true
